type Query {
    getBrand: Brand
    supportedLocales: [Locale]
}

type Brand {
    name: String
    logo: String
    logoUrl: String
    localization: Localization
}

type Localization {
    supportedLocales: [Locale]
    locale: String
}

type Locale {
    name: String
    code: String
}